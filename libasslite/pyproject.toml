[project]
name = "libasslite"
version = "0.1.0"
description = "Small runtime-loaded PyO3 binding for libass image layers"
readme = "README.md"
requires-python = ">=3.13"
license = "MIT"
license-files = ["LICENSE", "NOTICE", "THIRD_PARTY_LICENSES"]
dependencies = []

[build-system]
requires = ["maturin>=1.14,<2"]
build-backend = "maturin"

[tool.maturin]
module-name = "libasslite.libasslite"
python-source = "python"
